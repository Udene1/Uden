'use client';
import { useEffect, useMemo, useRef, useState } from 'react';
import { CheckCircle2, CircleAlert, Clock3, Loader2, Ban, ArrowRight, RefreshCw } from 'lucide-react';

type Node={id:string;title:string;status:string;selectedModel?:string;qualityScore?:number;costCents?:number;tokensIn?:number;tokensOut?:number;dependencies:string[];attemptedModels?:string[];output?:string;error?:string};
type Attempt={id?:string;node_id:string;attempt_number:number;model:string;provider?:string;status:string;prompt_tokens?:number;completion_tokens?:number;cost_cents?:number;quality_score?:number;escalation_reason?:string;error?:string;started_at?:string;completed_at?:string};
type Props={graph:{id:string;goal:string;status?:string;nodes:Node[]};attempts?:Attempt[];onRefresh?:()=>void;onResume?:()=>void;resuming?:boolean};
const icons:any={completed:CheckCircle2,running:Loader2,failed:CircleAlert,blocked:Ban,pending:Clock3,ready:Clock3};
function depthOf(node:Node,nodes:Node[],seen=new Set<string>()):number{if(!node.dependencies.length)return 0;if(seen.has(node.id))return 0;seen.add(node.id);return 1+Math.max(...node.dependencies.map(id=>{const d=nodes.find(n=>n.id===id);return d?depthOf(d,nodes,new Set(seen)):0;}));}
function fmtTime(v?:string){return v?new Date(v).toLocaleTimeString([], {hour:'2-digit',minute:'2-digit',second:'2-digit'}):'—'}
function duration(a:Attempt){if(!a.started_at||!a.completed_at)return '—';return `${Math.max(0,Date.parse(a.completed_at)-Date.parse(a.started_at))}ms`}
export default function TaskGraphView({graph,attempts=[],onRefresh,onResume,resuming}:Props){
 const [selected,setSelected]=useState(graph.nodes[0]?.id); const [positions,setPositions]=useState<Record<string,{x:number;y:number}>>({}); const wrap=useRef<HTMLDivElement>(null);
 const node=graph.nodes.find(n=>n.id===selected); const cost=graph.nodes.reduce((s,n)=>s+(n.costCents||0),0); const tokens=graph.nodes.reduce((s,n)=>s+(n.tokensIn||0)+(n.tokensOut||0),0);
 const columns=useMemo(()=>{const map=new Map<number,Node[]>();for(const n of graph.nodes){const d=depthOf(n,graph.nodes);map.set(d,[...(map.get(d)||[]),n]);}return Array.from(map.entries()).sort((a,b)=>a[0]-b[0]);},[graph.nodes]);
 useEffect(()=>{const update=()=>{const root=wrap.current;if(!root)return;const r=root.getBoundingClientRect();const next:Record<string,{x:number;y:number}>={};root.querySelectorAll<HTMLElement>('[data-node-id]').forEach(el=>{const b=el.getBoundingClientRect();next[el.dataset.nodeId!]={x:b.left-r.left+b.width/2,y:b.top-r.top+b.height/2};});setPositions(next)};update();window.addEventListener('resize',update);return()=>window.removeEventListener('resize',update)},[graph.nodes,selected]);
 const edges=graph.nodes.flatMap(n=>n.dependencies.map(d=>({from:d,to:n.id}))).filter(e=>positions[e.from]&&positions[e.to]);
 const nodeAttempts=attempts.filter(a=>a.node_id===selected);
 return <div className="space-y-6"><div className="flex flex-wrap gap-2 items-center"><button onClick={onRefresh} className="px-3 py-2 rounded-lg border border-[var(--border-color)] text-sm flex items-center gap-2"><RefreshCw size={15}/>Refresh</button>{(graph.status==='failed'||graph.status==='blocked')&&<button disabled={resuming} onClick={onResume} className="px-3 py-2 rounded-lg border border-[var(--accent-primary)] text-sm text-[var(--accent-primary)] flex items-center gap-2">{resuming?<Loader2 size={15} className="animate-spin"/>:<ArrowRight size={15}/>}Resume execution</button>}</div>
 <div className="grid grid-cols-2 md:grid-cols-4 gap-3"><Metric label="Status" value={graph.status||'unknown'}/><Metric label="Nodes" value={String(graph.nodes.length)}/><Metric label="Cost" value={`$${(cost/100).toFixed(4)}`}/><Metric label="Tokens" value={tokens.toLocaleString()}/></div>
 <div ref={wrap} className="glass-card border border-[var(--border-color)] p-6 overflow-auto relative min-h-[360px]"><svg className="absolute inset-0 w-full h-full pointer-events-none overflow-visible">{edges.map(e=><line key={`${e.from}-${e.to}`} x1={positions[e.from].x} y1={positions[e.from].y} x2={positions[e.to].x} y2={positions[e.to].y} stroke="currentColor" strokeOpacity=".35" strokeWidth="2" markerEnd="url(#arrow)"/>)}<defs><marker id="arrow" markerWidth="8" markerHeight="8" refX="7" refY="3" orient="auto"><path d="M0,0 L0,6 L7,3 z" fill="currentColor" opacity=".5"/></marker></defs></svg><div className="relative flex gap-12 min-w-max items-start">{columns.map(([depth,nodes])=><div key={depth} className="w-64 space-y-4"><div className="text-xs uppercase tracking-wide text-[var(--text-muted)]">Stage {depth+1}</div>{nodes.map(n=><button data-node-id={n.id} key={n.id} onClick={()=>setSelected(n.id)} className={`w-64 text-left rounded-xl border p-4 bg-[var(--bg-primary)] relative z-10 ${selected===n.id?'border-[var(--accent-primary)]':'border-[var(--border-color)]'}`}><div className="flex justify-between"><span className="font-mono text-xs text-[var(--text-muted)]">{n.id}</span><Status status={n.status}/></div><div className="font-semibold mt-2 text-[var(--text-primary)]">{n.title}</div><div className="text-xs mt-2 text-[var(--text-secondary)]">{n.selectedModel||'Model pending'}</div><div className="text-xs mt-1 text-[var(--text-muted)]">{n.qualityScore!=null?`${n.qualityScore}/100`:'Quality pending'} · $${((n.costCents||0)/100).toFixed(4)}</div>{n.dependencies.length>0&&<div className="text-xs mt-2 text-[var(--text-muted)]">Depends on: {n.dependencies.join(', ')}</div>}</button>)}</div>)}</div></div>
 {node&&<div className="glass-card border border-[var(--border-color)] p-6"><div className="flex justify-between"><div><h3 className="font-bold text-lg text-[var(--text-primary)]">{node.title}</h3><div className="font-mono text-xs text-[var(--text-muted)] mt-1">{node.id}</div></div><Status status={node.status}/></div><div className="grid md:grid-cols-4 gap-4 mt-5"><Metric label="Model" value={node.selectedModel||'—'}/><Metric label="Quality" value={node.qualityScore!=null?`${node.qualityScore}/100`:'—'}/><Metric label="Attempts" value={String(nodeAttempts.length||node.attemptedModels?.length||0)}/><Metric label="Latency" value={(()=>{const xs=nodeAttempts.filter(a=>a.started_at&&a.completed_at);return xs.length?`${xs.reduce((s,a)=>s+Date.parse(a.completed_at!)-Date.parse(a.started_at!),0)}ms`:'—'})()}/></div>{node.error&&<div className="mt-4 p-3 rounded-lg border border-red-500/30 text-red-400 text-sm">{node.error}</div>}{node.output&&<pre className="mt-4 max-h-80 overflow-auto whitespace-pre-wrap bg-[var(--bg-secondary)] p-4 rounded-lg text-sm text-[var(--text-secondary)]">{node.output}</pre>}</div>}
 <div className="glass-card border border-[var(--border-color)] p-6"><div className="font-bold text-lg text-[var(--text-primary)]">Execution timeline</div><div className="mt-4 overflow-x-auto"><table className="w-full text-sm"><thead><tr className="text-left text-[var(--text-muted)]"><th className="p-2">Time</th><th className="p-2">Node</th><th className="p-2">Attempt</th><th className="p-2">Model</th><th className="p-2">Status</th><th className="p-2">Latency</th><th className="p-2">Cost</th></tr></thead><tbody>{[...attempts].sort((a,b)=>(a.started_at||'').localeCompare(b.started_at||'')).map(a=><tr key={a.id||`${a.node_id}-${a.attempt_number}`} className="border-t border-[var(--border-color)]"><td className="p-2 text-[var(--text-muted)]">{fmtTime(a.started_at)}</td><td className="p-2">{a.node_id}</td><td className="p-2">#{a.attempt_number}</td><td className="p-2">{a.model}</td><td className="p-2">{a.status}</td><td className="p-2">{duration(a)}</td><td className="p-2">${((a.cost_cents||0)/100).toFixed(4)}</td></tr>)}</tbody></table>{attempts.length===0&&<div className="text-sm text-[var(--text-muted)] py-4">No model attempts recorded yet.</div>}</div></div>
 </div>;
}
function Status({status}:{status:string}){const I=icons[status]||Clock3;return <I size={17} className="text-[var(--accent-primary)]"/>}
function Metric({label,value}:{label:string;value:string}){return <div className="glass-card border border-[var(--border-color)] p-3"><div className="text-xs text-[var(--text-muted)]">{label}</div><div className="text-sm font-semibold mt-1 text-[var(--text-primary)]">{value}</div></div>}
