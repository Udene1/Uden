export async function getGraphs(){const r=await fetch('/api/backend/tasks/graphs',{cache:'no-store'});if(!r.ok)throw new Error(`Graph API error: ${r.status}`);return r.json() as Promise<{graphs:any[]}>;}
export async function getGraph(id:string){const r=await fetch(`/api/backend/tasks/graph/${encodeURIComponent(id)}`,{cache:'no-store'});if(!r.ok)throw new Error(`Graph API error: ${r.status}`);return r.json() as Promise<{graph:any}>;}
export async function getGraphAttempts(id:string,nodeId?:string){const q=nodeId?`?nodeId=${encodeURIComponent(nodeId)}`:'';const r=await fetch(`/api/backend/tasks/graph/${encodeURIComponent(id)}/attempts${q}`,{cache:'no-store'});if(!r.ok)throw new Error(`Graph API error: ${r.status}`);return r.json() as Promise<{attempts:any[]}>;}
export async function resumeGraph(id:string){const r=await fetch(`/api/backend/tasks/graph/${encodeURIComponent(id)}/resume`,{method:'POST'});const data=await r.json().catch(()=>({}));if(!r.ok)throw new Error(data.error||`Graph resume error: ${r.status}`);return data as {graph:any;status:string};}
