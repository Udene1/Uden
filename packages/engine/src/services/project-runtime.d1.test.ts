import { describe, expect, it, beforeAll, afterAll } from 'vitest';
import { getPlatformProxy } from 'wrangler';
import type { D1Database } from '@cloudflare/workers-types';
import { readFile } from 'node:fs/promises';
import { fileURLToPath } from 'node:url';
import { dirname, resolve } from 'node:path';
import { listProjectFiles, searchProjectFiles, upsertProjectFile, runProjectCommand, getProjectRuntimeJob } from './project-runtime';
import { applyProjectPatch } from './project-patch';
import { applyCurrentD1Schema } from '../test/d1-bootstrap';
const testDir = dirname(fileURLToPath(import.meta.url)); const engineRoot = resolve(testDir, '../..'); const schemaPath = resolve(testDir, '../db/schema.sql'); const migration = resolve(engineRoot, 'migrations/0007_workspace_runtime.sql'); const patchMigration = resolve(engineRoot, 'migrations/0009_project_patches.sql');
const execSqlFile = async (db:D1Database,path:string) => { const sql=await readFile(path,'utf8'); for(const statement of sql.replace(/^\uFEFF/,'').replace(/^[\t ]*--[^\r\n]*(?:\r?\n|$)/gm,'').split(';').map(s=>s.trim()).filter(Boolean)) await db.prepare(statement).run(); };
describe('project workspace D1 integration',()=>{ let db:D1Database; let dispose:(()=>Promise<void>)|undefined;
 beforeAll(async()=>{const platform=await getPlatformProxy({configPath:resolve(engineRoot,'wrangler.test.jsonc'),persist:false});db=platform.env.DB as D1Database;dispose=platform.dispose;await execSqlFile(db,schemaPath);await execSqlFile(db,migration);await execSqlFile(db,patchMigration);await applyCurrentD1Schema(db,engineRoot);await db.prepare('INSERT INTO tenants (id,name,email,api_key_hash) VALUES (?,?,?,?)').bind('workspace-a','Workspace A','a@example.test','workspace-hash-a').run();await db.prepare('INSERT INTO tenants (id,name,email,api_key_hash) VALUES (?,?,?,?)').bind('workspace-b','Workspace B','b@example.test','workspace-hash-b').run();await db.prepare('INSERT INTO projects (id,tenant_id,name) VALUES (?,?,?)').bind('project-a','workspace-a','Project A').run();await db.prepare('INSERT INTO projects (id,tenant_id,name) VALUES (?,?,?)').bind('project-b','workspace-b','Project B').run();});
 afterAll(async()=>{await dispose?.();});
 it('persists versioned files with tenant isolation',async()=>{const first=await upsertProjectFile({DB:db} as never,'workspace-a','project-a','src/index.ts','export const answer = 42;',0);expect(first.version).toBe(1);const second=await upsertProjectFile({DB:db} as never,'workspace-a','project-a','src/index.ts','export const answer = 43;',1);expect(second.version).toBe(2);expect((await listProjectFiles({DB:db} as never,'workspace-a','project-a')).map(file=>file.path)).toEqual(['src/index.ts']);expect(await listProjectFiles({DB:db} as never,'workspace-b','project-a')).toEqual([]);});
 it('searches project code without crossing tenants',async()=>{await upsertProjectFile({DB:db} as never,'workspace-a','project-a','src/search.ts','const target = "needle";\nconst other = 1;',0);await upsertProjectFile({DB:db} as never,'workspace-b','project-b','src/other.ts','const target = "needle";',0);const matches=await searchProjectFiles({DB:db} as never,'workspace-a','project-a','NEEDLE');expect(matches).toEqual([{path:'src/search.ts',line:1,text:'const target = "needle";'}]);expect(await searchProjectFiles({DB:db} as never,'workspace-b','project-a','needle')).toEqual([]);});
 it('applies version-checked patches and records patch history',async()=>{const file=await upsertProjectFile({DB:db} as never,'workspace-a','project-a','src/patch.ts','const value = 1;',0);const result=await applyProjectPatch({DB:db} as never,'workspace-a','project-a',[{path:'src/patch.ts',content:'const value = 2;',expectedVersion:file.version}]);expect(result[0].version).toBe(2);const history=await db.prepare('SELECT path,base_version,status FROM project_patches WHERE tenant_id=? AND project_id=?').bind('workspace-a','project-a').all<{path:string;base_version:number;status:string}>();expect(history.results).toContainEqual({path:'src/patch.ts',base_version:1,status:'applied'});await expect(applyProjectPatch({DB:db} as never,'workspace-a','project-a',[{path:'src/patch.ts',content:'stale',expectedVersion:1}])).rejects.toThrow('Project file version conflict');});
 it('rejects path traversal and optimistic concurrency conflicts',async()=>{await expect(upsertProjectFile({DB:db} as never,'workspace-a','project-a','../secrets','x',0)).rejects.toThrow('Invalid project file path');await expect(upsertProjectFile({DB:db} as never,'workspace-a','project-a','src/index.ts','bad',1)).rejects.toThrow('Project file version conflict');});
 it('fails closed when project runtime has no execution fence',async()=>{const env={DB:db} as never;await expect(runProjectCommand(env,'workspace-a','project-a','npm test',[])).rejects.toThrow('Execution fence is required for project runtime execution');await expect(getProjectRuntimeJob(env,'workspace-a','project-a','job-1')).rejects.toThrow('Project runtime is not configured');});
 it('continues from a terminal durable runtime execution without replaying transport',async()=>{await db.prepare(`INSERT INTO tenants (id,name,email,api_key_hash,monthly_budget_cents) VALUES (?,?,?,?,?)`).bind('runtime-continuation-tenant','Runtime continuation','runtime-continuation@example.test','runtime-continuation-hash',100).run();await db.prepare(`INSERT INTO tasks (id,tenant_id,prompt,status) VALUES (?,?,?,?)`).bind('runtime-continuation-root','runtime-continuation-tenant','runtime continuation','processing').run();await db.prepare(`INSERT INTO task_graphs (id,tenant_id,root_task_id,goal,status,execution_owner,execution_version,lease_until) VALUES (?,?,?,?,?,?,?,datetime('now','-1 second'))`).bind('runtime-continuation-graph','runtime-continuation-tenant','runtime-continuation-root','runtime continuation','awaiting-runtime','old-worker',1).run();await db.prepare(`INSERT INTO runtime_executions (id,tenant_id,runtime_id,graph_id,node_id,attempt_id,execution_version,execution_owner,capability,status,stdout,exit_code,started_at,finished_at) VALUES (?,?,?,?,?,?,?,?,?,?,?,?,?,?)`).bind('runtime-terminal-attempt','runtime-continuation-tenant','desktop-local','runtime-continuation-graph','node-1','runtime-terminal-attempt',1,'old-worker','command.exec','completed','done',0,new Date().toISOString(),new Date().toISOString()).run();const result=await getProjectRuntimeJob({DB:db} as never,'runtime-continuation-tenant','project-unused','runtime:runtime-terminal-attempt');expect(result.status).toBe('succeeded');expect(result.output).toBe('done');});
});
